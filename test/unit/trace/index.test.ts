import { trace, trace_float, trace_num } from '../../../src/api'

describe.todo('trace', () => {
  it('trace', () => {
    trace(0, 0, 0, 0, 0, {} as any)
  })

  it('trace_num', () => {
    trace_num(0, 0, 0, {} as any)
  })

  it('trace_float', () => {
    trace_float(0, 0, 0, {} as any)
  })
})
